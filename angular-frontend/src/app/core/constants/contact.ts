/** Central contact strings so templates avoid raw `@` (Angular control-flow parsing). */
export const SUPPORT_EMAIL = 'support@aiadsgenerator.com';
export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}`;
export const PRIVACY_EMAIL = 'privacy@aiadsgenerator.com';
export const PRIVACY_MAILTO = `mailto:${PRIVACY_EMAIL}`;
