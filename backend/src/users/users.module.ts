/**
 * User profile, avatar (Cloudinary), sessions, email change, account deletion.
 * Routes: `src/users/users.routes.ts` mounted at `/users` in `src/index.ts`.
 */
export const USERS_MODULE_MARKER = true;
